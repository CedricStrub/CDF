{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}

<link rel="stylesheet" href={{ asset("/formation.css") }}>
<div class="forma">
    <div class="title-ctn">
        <div class="title">{{ utilisateur.nom }} - {{ utilisateur.prenom }}</div>
    </div>
    <div class="ctn-tab">
        <div class="cadre-tab">
            <div class="titre">Liste des Sessions</div>
            <div class="tab">
                <table>
                    <thead>
                        <td>Intitulé</td>
                        <td>Date de Début</td>
                        <td>Date de Fin</td>
                        <td>Places</td>
                    </thead>
                    <tbody>
                    {% for session in sessions %}
                        <tr>
                            <td><a href="{{ path('show_session', {'id': session.id }) }}">{{ session }}</a></td>
                            <td>{{ session.dateDebut | date("d-m-Y") }}</td>
                            <td>{{ session.dateFin | date("d-m-Y") }}</td>
                            {% if session.getParticiper is iterable %}
                            <td>{{ session.getParticiper | length }} / {{ session.place }}</td>
                            {% else %}
                            <td>0 / {{ session.place }}</td>
                            {% endif %}
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

{% endblock %}